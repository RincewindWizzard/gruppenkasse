(function () {
    /*function loadContent(url) {
        $("#content").html(url);
    }

    $( document ).ready(function() {
        // Loading the active Tab
        loadContent($("ul.nav .active a").attr("href"));
        
        // Clicking another tab changes the content
        $('li a, .navbar-brand').click(function(event) {
            //event.preventDefault();
            
            $('div.collapse.navbar-collapse li').removeClass("active");
            if($(this).hasClass("navbar-brand")) {
            }
            else {
               $(this).parent().addClass("active");
            }
            loadContent($(this).attr("href"));
        });
    });*/
})();
